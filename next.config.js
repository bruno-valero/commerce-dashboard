/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'cdn.shopclues.com', 
      'hips.hearstapps.com',
      'images.immediate.co.uk',
      'images.unsplash.com',
    ]
  }
}

module.exports = nextConfig
